export function FoundationPage() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6">
      <section className="w-full max-w-2xl rounded-lg border bg-white p-10 shadow-sm dark:bg-slate-950">
        <p className="text-sm font-semibold uppercase tracking-[0.2em] text-blue-700 dark:text-blue-300">
          Phase 2 foundation
        </p>
        <h1 className="mt-4 text-4xl font-semibold tracking-tight">CloseoutFlow is operational.</h1>
        <p className="mt-4 max-w-xl text-lg leading-8 text-slate-600 dark:text-slate-300">
          The repository foundation is ready for architecture verification. Product features and the
          final interface intentionally begin in later phases.
        </p>
      </section>
    </main>
  );
}

export default FoundationPage;
